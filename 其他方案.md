# Linux 上的 Windows Recall / Click to Do 等效方案：现有项目调研与自建设计可行性分析

## TL;DR

- 现有 Recall-like 项目里，**没有一个能在 KWin Wayland 上开箱即用**：OpenRecall（AGPLv3，GitHub 仓库页显示 2.8k stars、183 forks）用 python-mss，在 Wayland 上直接抛 `XGetImage() failed`；screenpipe（约 16k stars，已改为 "Screenpipe Commercial License" 的 source-available 许可、非 OSI open source，签名桌面版订阅制起价 $25/月）用 xcap/xcb crate，官方 issue 承认只支持 X11 / XWayland、抓不到 Wayland 原生窗口；Windrecorder 只支持 Windows。因此在你的 Arch + Plasma 6 + KWin Wayland 环境上，要么切 X11 session，要么自建。
- **Click to Do 类（选中屏上任意元素 / 图中文字）在 Linux 上是成熟的**：Normcap、Frog、TextSnatcher 都能用，走 xdg-desktop-portal 的 Screenshot 接口 + Tesseract，在 KDE Wayland 可用（有一次授权弹窗）。
- **你设想的 systemd + 脚本极简方案是可行且推荐的方向**，但每个环节都有 Wayland 特有的坑：截图必须走 `spectacle -b -n` 且带 `--desktopfile` 授权（grim 在 KWin 上不工作），active window 只能靠 KWin scripting（kdotool/kwst），浏览器 URL 最稳的是 Plasma Browser Integration 的 D-Bus，Anki 用 AnkiConnect HTTP，Okular/Kitty 各有 D-Bus / remote control 接口。核心建议：**自建采集脚本，但索引层直接复用成熟组件（SQLite FTS5 + RapidOCR），不要从零写 UI。**

## Key Findings

### Part 1 现有项目对比结论

1. **OpenRecall** — AGPLv3，真正 open source，GitHub 仓库页显示 2.8k stars / 183 forks。采集用 python-mss（setup.py 里 `mss==9.0.1`），Wayland 下必然失败：issue #10（micush，2024-06-05，"MSS appears to only support X11"）和 issue #67 都有 KDE Plasma 6.0.5 Wayland 的 `XGetImage() failed` traceback。OCR 用 doctr（maintainer koenvaneijk 的 fork，setup.py 里的 python-doctr extra 可确认；README 宣称 "Advanced local OCR"，但 Tesseract 是否为硬依赖未从 setup.py 证实，标注待确认）。索引 SQLite + embedding，**默认无加密**（README 只建议放在加密卷 / 可移动加密盘；Beebom 实测证实截图与索引均无加密，DB 可直接用 SQLite Browser 打开）。结论：适合 X11，不适合你的 Wayland。

2. **screenpipe** — 约 16k stars（GitHub live 数据 16.2k，官方 press kit 称 18.5k，有出入），非常活跃（2026 年仍在 v2.7.x 频繁发版）。许可 2026 年已从 MIT 改成 "Screenpipe Commercial License"（source-available，个人非商业免费、商业需授权，见 LICENSE.md；签名桌面版订阅制起价 $25/月），**不再是 OSI open source**。架构比 OpenRecall 先进：event-driven 采集（app 切换 / 点击 / 停顿 / 滚动才截图）、accessibility tree 优先 + OCR fallback、SQLite FTS5、REST API on localhost:3030。但 Linux 采集用 xcap（底层 libxcb），官方 issue #1338 明说只在 X11 / XWayland 工作、"do not detect wayland apps"，建议 Linux 用户切到 X11 session；未见针对 KWin/Plasma 6 Wayland 的确认可用证据（PR #477 曾尝试改善 Wayland 但无可靠 Plasma 6 抓屏确认）。自报开销 CPU 5-10%、磁盘 5-10 GB/月（README vendor 数字，非独立基准）。

3. **Windrecorder** — 依赖 Windows API，只支持 Windows，Linux 不可用。

4. **LibreRecall** — 明确为 "OpenRecall 在 Wayland KDE Arch 上不工作" 而生的小项目，每 30 秒截图 + Tesseract 存同名 txt，非常粗糙且已声明停止开发（转 LibreRecall II）。仅作参考，不建议依赖。

5. **ActivityWatch** — 不是 Recall（不存截图，只记录窗口标题 / URL / AFK），但其 window watcher 思路对你有用。关键：**官方 aw-watcher-window 在 KWin Wayland 上不工作**（官方 FAQ 明说 "Wayland protocol does not have a notion of an active window, and it is unlikely to ever have"）。可用替代是 **awatcher（2e3s/awatcher）**，带 `kwin_window` feature，通过 KWin scripting 取窗口；浏览器 URL 靠官方浏览器扩展。

6. **Click to Do 类**：Normcap（跨平台，X11+Wayland，Tesseract + wl-clipboard，AUR/Flatpak 有包）、Frog、TextSnatcher（Linux OCR 取词）。这些是交互式框选，不是后台连续采集，正好补足 Recall 缺的 "选中图中文字" 能力，可与自建后台方案并存。

### Part 2 自建方案可行性

**(a) 截图（KWin Wayland）** — 唯一稳的非交互式全屏截图是 `spectacle -b -n -o file.png`（`-b` background、`-n` 无通知）。三个坑：
- 历史 bug 425641：`-b -n -o` 曾把图存到剪贴板而非文件（老版本），新版已修，**务必在你的 Plasma 6.x 上实测落盘**。
- **授权**：KWin 的 `org.kde.KWin.ScreenShot2` / `org.kde.KWin.CaptureScreen` D-Bus 接口受限，进程必须有一个 `.desktop` 文件带 `X-KDE-DBUS-Restricted-Interfaces=org.kde.KWin.ScreenShot2,org.kde.KWin.CaptureScreen` 和 `X-KDE-Wayland-Interfaces=org_kde_plasma_window_management,zkde_screencast_unstable_v1`，且调用带 `--desktopfile org.kde.spectacle.desktop`，否则报 `org.kde.KWin.ScreenShot2.Error.NoAuthorized`。若想绕过 spectacle 直接调 D-Bus，需把一个授权的 .desktop 放到 `/usr/share/applications` 或 `~/.local/share/applications`。
- **grim / wlr-screencopy 在 KWin 上不工作**：KWin 不实现 wlr-screencopy（grim 仅支持 wlroots），ext-image-copy-capture 在部分 KWin 版本也未暴露（Arch 论坛与多个 issue 均确认）。别指望 grim。
- systemd：用 **timer（`OnUnitActiveSec=10s` + `AccuracySec=1s`）** 比常驻 loop 更省资源、更 auditable，但 10s 高频反复拉起进程有 fork/spectacle 启动开销，常驻脚本 loop 也可接受、启动更快。单元须 `PartOf=graphical-session.target`、`WantedBy=graphical-session.target`，并确保 `WAYLAND_DISPLAY` 与 `DBUS_SESSION_BUS_ADDRESS` 已注入（Plasma 的 graphical-session 通常已 import；否则 `systemctl --user import-environment`）。

**(b) 变化检测去重** — OCR 前先去重是省 CPU / 省磁盘的关键。推荐 **dHash（difference hash）**，最便宜、对小噪声鲁棒；Python `imagehash` 或 Rust `img_hash` crate 均可。阈值需实测（dHash 64-bit 下 Hamming distance ≤ 3-5 视为相同）。坑：屏幕时钟、光标、视频播放、滚动会让近乎相同的画面每帧 "变化"，使去重失效 —— spectacle 默认不含光标（好事），时钟可做 per-region mask，视频窗口可靠 window class 排除或提高阈值。ImageMagick `compare -metric` 能算 SSIM/差异但比 hash 慢。

**(c) OCR + 索引** — 中英混合 + 数学公式的论文 / slides：
- **Tesseract**：100+ 语言、生态成熟，但 CJK 和公式弱，需预处理。
- **PaddleOCR / RapidOCR**：多来源与 arXiv 基准一致指出 Paddle 系 CJK 精度最强。**RapidOCR（ONNX Runtime，Apache-2.0）是最佳选择**：用 Paddle 的训练模型但去掉 PaddlePaddle 框架依赖，安装约 50-80 MB，CPU 每页约 0.5-1s，中英默认支持，最轻量、最易审计部署。
- 数学公式：通用 OCR 都不擅长，需专门模型（pix2tex / LaTeX-OCR），建议作可选增强、不进主 pipeline。
- 索引：**SQLite FTS5**。中文分词是核心问题 —— 默认 `unicode61` 把整句当一个 token，中文搜索基本失效。选项：`trigram`（3.34.0+，子串匹配、索引膨胀约 3 倍、丢失相关性排序，2 字词无法索引需 LIKE fallback）；`simple`（wangfenjin/simple，支持中文 + 拼音，带 `simple_query()`）；或 jieba 分词后入 FTS5。论文场景推荐 simple 或 jieba。可选 embedding 语义搜索作二级检索。

**(d) Active window + 每应用上下文**：
- **Active window**：Wayland 无全局 API。KWin 走 scripting API —— **kdotool**（`kdotool getactivewindow getwindowname`，Plasma 6，动态注入 KWin script 经 D-Bus）或 **kwst**（`kwst get-active-window`）。这与 ActivityWatch awatcher 同思路。注意 KWin caption 限 255 字符。
- **Dolphin**：D-Bus 不可靠地暴露当前目录。更稳：开启 Dolphin "标题栏显示完整路径" 后读窗口标题，或用 baloo / KActivities recent documents。
- **浏览器 URL**：最稳是 **Plasma Browser Integration** 的 D-Bus：`qdbus org.kde.plasma.browser_integration /TabsRunner org.kde.plasma.browser_integration.TabsRunner.GetTabs` 返回所有 tab 的 title + url（Firefox / Chromium 装扩展即可）。局限：返回所有 tab、不直接标 active tab（需结合 active window 是浏览器 + 标题匹配来定位）。备选：MPRIS（仅播放媒体时）、读 Firefox `sessionstore recovery.jsonlz4`（有延迟）、Chrome DevTools protocol（需 `--remote-debugging-port`，侵入性大）、窗口标题（最脆但零依赖）。综合评估：Plasma Browser Integration 最 robust。
- **Anki**：**AnkiConnect**（HTTP on `localhost:8765`，非 D-Bus）。`guiCurrentCard` 返回 `answer`、`question`、`deckName`、`modelName`、`fields`（每 field 的 value + order）、`cardId`、`buttons`、`template`、`css` 等，**仅在 review 模式有效**（否则抛 "Gui review is not currently active"，源码 `guiReviewActive()` 判 `window().state == 'review'`）。浏览时用 `guiBrowse`（搜索返回 card ids）、`cardsInfo`（取详情）、`guiSelectedNotes`。
- **Okular**：D-Bus `org.kde.okular-{PID}` 对象 `/okular`，接口 `org.kde.okular`，方法 `currentDocument`（路径）、`currentPage`。坑：多 tab 时 `currentPage` 只返回最初打开的文档（bug 502482），单 tab 可靠。
- **Kitty**：`kitty @ ls` 返回 JSON，含每个 window 的 `cwd`、`pid`、`cmdline`、`foreground_processes`、`is_focused`、`env`。需 kitty.conf `allow_remote_control yes`。注意多前台进程时 cwd 取最高 PID（0.24.3 后行为）。
- **Zotero**：Better BibTeX 提供本地 JSON-RPC / API，可查当前条目（具体端点未在本次逐一验证，标注待确认）。

**(e) 架构 / 开销 / 安全**：
- 资源：10s 一帧 = 8640 帧/天。去重后实际存储与 OCR 量大幅下降（screenpipe 自报 event-driven 后 8 小时约 300 MB vs 连续录制 2 GB）。OCR 是主要 CPU 成本，去重后每天真正 OCR 帧数取决于内容切换频率。
- 安全：**Windows Recall 的教训非常直接**。初版把截图和 OCR 文本存在**未加密的 SQLite 明文库**，登录状态下任何用户态进程可读：TotalRecall 作者 Alex Hagenah 称其 "The database is unencrypted. It's all plain text… It's a Trojan 2.0 really, built in"，Kevin Beaumont 称 Recall "sets cybersecurity back a decade" 并指出数据仅在注销时加密。Microsoft 因此于 2024-06-13 取消原定 6 月 18 日的 Copilot+ 广泛预览、先给 Windows Insider，并新增 "just in time" decryption（受 Windows Hello Enhanced Sign-in Security 保护）+ VBS Enclave，累计推迟逾一年直到 2025 年 4 月才广泛上线；即便如此，2026 年研究员 xaitax 的 TotalRecall Reloaded 仍向渲染进程 AIXHost.exe 注入 DLL（无需管理员）读取用户认证后已解密的截图/OCR 文本（"The vault door is titanium. The wall next to it is drywall"），Microsoft 回应称 "matches intended design / not a vulnerability"。**给你的教训**：(1) 数据库和图片必须加密（至少 LUKS / 加密卷，或应用层加密）；(2) 必须能排除密码管理器窗口、隐私 / 无痕窗口、银行页面 —— 靠 active window 的 class / title 做 exclusion list，命中即跳过截图；(3) 明文索引 = 一个集中的高价值失窃目标，且登录态加密无法防同用户进程。
- 自建 vs 复用：
  - 从零写全部（含 UI / 语义搜索）不划算。
  - **推荐分层**：采集层自己写（systemd + spectacle + kdotool + 各 D-Bus 上下文，完全 auditable，符合你偏好）；OCR 用 RapidOCR；索引 / 检索用 SQLite FTS5（simple 或 jieba tokenizer）+ 一个薄查询脚本 / 简单 web UI。
  - 若愿意接受 X11 session，可直接跑 OpenRecall（AGPLv3）省事；但你在 Wayland 且偏好可审计，自建采集 + 成熟索引组件是最佳平衡。
  - screenpipe 因许可（source-available、商业受限、桌面版订阅）+ Linux Wayland 采集不支持，不推荐作为基座。

## Details

具体命令、D-Bus 路径、bug 编号、许可与 stars 已在 Key Findings 各条给出。要点重述：
- 截图授权 `.desktop` 关键行：`X-KDE-DBUS-Restricted-Interfaces=org.kde.KWin.ScreenShot2,org.kde.KWin.CaptureScreen` + `X-KDE-Wayland-Interfaces=org_kde_plasma_window_management,zkde_screencast_unstable_v1`；调用 `spectacle --desktopfile org.kde.spectacle.desktop -b -n -o <file>`。
- active window：`kdotool getactivewindow getwindowname` / `kdotool getactivewindow getwindowclassname`。
- 浏览器：`qdbus org.kde.plasma.browser_integration /TabsRunner org.kde.plasma.browser_integration.TabsRunner.GetTabs`。
- Anki：`curl localhost:8765 -d '{"action":"guiCurrentCard","version":6}'`。
- Okular：`qdbus org.kde.okular-<PID> /okular org.kde.okular.currentDocument`。
- Kitty：`kitty @ ls`（JSON，取 `is_focused` window 的 `cwd` / `foreground_processes`）。

## Recommendations

1. **先验证截图链路（第一优先，1 小时内可判定可行性）**：写一个带授权的 `.desktop`，跑 `spectacle -b -n --desktopfile org.kde.spectacle.desktop -o /tmp/t.png`，确认在你的 Plasma 6.x 上能真正落盘（而非进剪贴板）。若不行，先解决这个再谈其他。基准：能稳定每 10s 落盘一张 PNG。
2. **加去重**：先算 dHash，Hamming distance 阈值实测（起始设 5），只对 "变化帧" 做 OCR。基准：日常办公一天真正 OCR 的帧数应远小于 8640；若接近，说明去重阈值或时钟 / 视频区域未处理好。
3. **OCR + 索引**：RapidOCR（中英）→ 文本入 SQLite FTS5，中文用 simple 或 jieba tokenizer。先不做 embedding，FTS 不够再加语义层。
4. **上下文采集**：kdotool 拿 active window class/title；命中 exclusion list（KeePassXC、浏览器隐私窗口、银行域名）就丢弃该帧。浏览器 URL 用 Plasma Browser Integration D-Bus；Anki 用 AnkiConnect `guiCurrentCard`；Okular / Kitty 各自接口。
5. **安全兜底**：库和图片放加密卷（LUKS）；exclusion list 默认从严；提供一键暂停 / 一键清除；意识到登录态下应用层再加密对同用户进程防护有限，物理 / 备份层加密才是主要保障。
6. **触发升级的阈值**：若磁盘 / CPU 超预算，转向 screenpipe 式 event-driven 采集（靠 active window 变化 / 输入停顿触发，替代固定 10s）。若中文检索质量不满意，从 trigram 换 jieba。若需要 "选中图中文字" 的即时能力，直接装 Normcap 补足，不必自建。

## Caveats

- `spectacle -b -n -o` 落盘行为随版本变化（老版本存剪贴板 bug 425641），务必实测。
- grim / wlr-screencopy 在 KWin 上不可用，是协议层事实，短期不变。
- screenpipe 的 CPU 5-10% / 磁盘 5-10 GB/月为厂商 README 自报，未经独立基准；其 stars 数各源不一（16.2k live vs 18.5k press kit）。
- OpenRecall 是否把 Tesseract 作为硬依赖未从 setup.py 证实（仅确认 doctr），标注待确认；其未公布具体 CPU/磁盘数字。
- Zotero / Better BibTeX 本地 API 端点未在本次逐一验证，标注待确认。
- Okular 多 tab 下 `currentPage` 返回错误文档（bug 502482），单 tab 可靠。
- 数学公式 OCR 通用引擎都弱，需专用模型，属可选增强。
- Anki `guiCurrentCard` 仅 review 时有效；浏览时改用 `guiBrowse` / `guiSelectedNotes`。
- Wayland active window 依赖 KWin scripting（动态注入 KWin script），属半官方机制，KWin 大版本升级可能改 API。
- screenpipe 针对 KWin/Plasma 6 Wayland 的确切抓屏状态未能找到具名 issue 证实，仅有 "xcap/xcb → X11 only、建议切 X11" 的一般性结论（issue #1338）。
